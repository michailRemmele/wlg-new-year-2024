import type { FC } from 'react';

export interface MoveControlProps {
  className: string
}

export const MoveControl: FC<MoveControlProps> = ({ className }) => (
  <div className={className}>TODO TOUCH MOVE CONTROL</div>
);
